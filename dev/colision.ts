/*
class colision {
    
    private c1:gameObject;
    private c2:gameObject;

    constructor(c1:gameobject, c2:gameObject) {
        this.c1 = c1;
        this.c2 = c2;

        collisionCheck(this.c1, this.c2);

    }

 public collisionCheck(c1, c2): boolean {
        return !(c2.x > c1.x + c1.width || 
                 c2.x + c2.width < c1.x || 
                 c2.y > c1.y + c1.height || 
                 c2.y + c2.height < c1.y);
    }




*/